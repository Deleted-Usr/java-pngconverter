package com.willclay.pngconverter.actions.view;

/// Background choices for the image preview area.
public enum PreviewBackground
{
    /// A solid dark background.
    DARK,
    /// A solid light background.
    LIGHT,
    /// A checkerboard that makes transparent pixels apparent.
    CHECKERBOARD
}
